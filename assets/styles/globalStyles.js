import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  backgroundWhite: {
    backgroundColor: '#fff',
  },
  flex: {
    flex: 1,
  },
});

export default globalStyles;
